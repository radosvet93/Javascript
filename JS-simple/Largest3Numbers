function max3Elements(arr){
    let nums = arr.map(Number);
    nums.sort((a,b) => b-a);
    let count = Math.min(3,nums.length);
    for(let i = 0; i < count;i++){
        console.log(nums[i]);
    }

}

max3Elements(["100","20","3","4","-6"]);
